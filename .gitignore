# 이 저장소는 mini_turtle4 패키지 하나만 올린다.
#
# src/ 에는 turtlebot4, m-explore-ros2 등 각자 .git을 가진 패키지가 더 있다.
# 그것들을 add하면 파일이 아니라 서브모듈 포인터(mode 160000)가 커밋돼서
# GitHub에 열리지 않는 빈 회색 폴더로 올라간다. 화이트리스트로 원천 차단한다.
#
# 세부 무시 규칙은 mini_turtle4/.gitignore 에 있다.

/*
!/.gitignore
!/mini_turtle4/
